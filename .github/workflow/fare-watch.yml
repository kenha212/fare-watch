name: Fare Watch

on:
  schedule:
    # Every 3 days at 22:00 UTC (~9am Sydney time, AEDT)
    - cron: "0 22 */3 * *"
  workflow_dispatch: {}

jobs:
  check-fares:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install -r fare-watch/requirements.txt

      - run: python fare-watch/fare_watch.py
        env:
          AMADEUS_API_KEY: ${{ secrets.AMADEUS_API_KEY }}
          AMADEUS_API_SECRET: ${{ secrets.AMADEUS_API_SECRET }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}

      - name: Commit updated price history
        run: |
          git config user.name "fare-watch-bot"
          git config user.email "actions@users.noreply.github.com"
          git add fare-watch/price_history.json
          git commit -m "Update fare history" || echo "No changes to commit"
          git push
